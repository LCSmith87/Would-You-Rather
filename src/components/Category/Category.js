import React from 'react'

// todo: future categories

const Category = (props) => {
    return (
        <div>
            {props.children}
        </div>
    )
}

export default Category
